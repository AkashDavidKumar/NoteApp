# =====================================================
# NoteApp Backend Environment Variables
# =====================================================
# Copy this file to .env and fill in your actual values
#
# IMPORTANT: Never commit .env file to git!
# Always use .env.example for reference
# =====================================================

# ===== MongoDB Configuration =====
# Your MongoDB connection string
# Get this from MongoDB Atlas or your local MongoDB setup
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string_here

# ===== Upstash Redis Configuration =====
# Required for rate limiting functionality
# Get these from your Upstash Console: https://console.upstash.com

# REST API URL for your Upstash Redis instance
UPSTASH_REDIS_REST_URL=https://your-upstash-redis-url.upstash.io

# REST API Token for authentication
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# ===== Server Configuration =====
# Port number for the Express server
# Default: 5001
PORT=5001

# Environment type (development or production)
# Set to 'production' when deploying to Render
# Development enables CORS for localhost:5173
NODE_ENV=development

# =====================================================
# Setup Instructions:
# =====================================================
#
# 1. MongoDB Atlas Setup:
#    - Create account at https://www.mongodb.com/cloud/atlas
#    - Create a free cluster
#    - Get the connection string (Replace <password> with your password)
#    - Paste in MONGODB_URI
#
# 2. Upstash Redis Setup:
#    - Create account at https://upstash.com
#    - Create a new Redis database
#    - Copy REST API URL and Token from the console
#    - Paste them in UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN
#
# 3. Local Development:
#    - Copy this file: cp .env.example .env
#    - Fill in your actual credentials
#    - Run: npm run dev
#
# 4. Production (Render):
#    - Set all environment variables in Render dashboard
#    - Make sure NODE_ENV=production is set
#    - Frontend will be served from backend/frontend/dist
#
# =====================================================
